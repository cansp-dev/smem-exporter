[package]
name = "smem-exporter"
version = "0.1.2"
edition = "2021"
authors = ["CANSP – Computer and Network Service Provider <info@cansp.net>"]
license = "MIT"
description = "Prometheus exporter for per-process memory metrics (RSS/PSS/USS)"
repository = "https://github.com/cansp-dev/smem-exporter"

[dependencies]
axum = "0.7"
clap = { version = "4.5", features = ["derive"] }
prometheus = "0.13"
rayon = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
tokio = { version = "1.37", features = ["full"] }
